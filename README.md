# Cosine-Similarity
It a model developed in JAVA which calculate cosine similarity between two text files. Just call Calculate_CosineSimilarity method of Calculate_tf_idf class with two file as parameter and the path of the folder containing them.
